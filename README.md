# blowfishDecryption
use blowFish and ECB to decrypt

A .NET 8 library that scans JSON files for values enclosed in ![...] and decrypts them using the Blowfish algorithm with ECB mode and PKCS7 padding. Designed for use in environment-specific configuration files like appsettings.json.